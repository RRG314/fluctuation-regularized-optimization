# Paper

`paper.tex` contains the manuscript source for the current
`fluctuation-regularized-optimization` naming.

The previous compiled PDF was removed because it used older project/API
naming. Rebuild the PDF from `paper.tex` with a LaTeX engine before attaching
a compiled manuscript to a release.
